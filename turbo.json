{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**","!.next/cache/**"],
      "dotEnv": [".env"]
    },
    "dev": {
      "cache": false,
      "dotEnv": [".env"]
    },
    "lint": {
      "outputs": [],
      "dotEnv": [".env"]
    },
    "format": {
      "outputs": [],
      "dotEnv": [".env"]
    }
  }
}